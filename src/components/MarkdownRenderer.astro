<div class="markdown-content max-w-3xl mx-auto">
    <slot />
</div>

<style is:global>
    @reference "../styles/global.css";
    
    /* Remove default margins from global styles within markdown */
    .markdown-content h1,
    .markdown-content h2,
    .markdown-content h3,
    .markdown-content h4,
    .markdown-content h5,
    .markdown-content h6 {
        @apply mb-0;
    }

    .markdown-content h1 {
        @apply text-5xl font-light tracking-tight leading-tight text-gray-900 mb-8 md:text-[2.75rem];
    }

    .markdown-content h2 {
        @apply text-2xl font-medium text-gray-900 mt-12 mb-6;
    }

    .markdown-content h3 {
        @apply text-xl font-medium text-gray-900 mt-10 mb-4;
    }

    .markdown-content h4,
    .markdown-content h5,
    .markdown-content h6 {
        @apply text-lg font-medium text-gray-900 mt-8 mb-3;
    }

    .markdown-content p {
        @apply text-lg font-light leading-relaxed text-gray-500 mb-6 last:mb-0;
    }

    .markdown-content pre {
        @apply p-4 mb-4;
    }

    .markdown-content a {
        @apply text-accent-600 font-medium hover:text-accent-700 transition-colors no-underline;
    }

    .markdown-content ul,
    .markdown-content ol {
        @apply ml-6 mb-6;
    }

    .markdown-content blockquote {
        @apply border-l-4 border-accent-200 pl-6 text-gray-600 my-8 italic;
    }

    .markdown-content li {
        @apply text-lg font-light leading-relaxed text-gray-500 list-inside mb-3 last:mb-0;
    }

    .markdown-content img {
        @apply max-w-full max-h-full w-auto h-auto block my-8;
    }
</style>
