<div class="markdown-content max-w-[70ch]">
    <slot />
</div>

<style is:global>
    @reference "../styles/global.css";
    
    /* Remove default margins from global styles within markdown */
    .markdown-content h1,
    .markdown-content h2,
    .markdown-content h3,
    .markdown-content h4,
    .markdown-content h5,
    .markdown-content h6 {
        @apply mb-0;
    }

    .markdown-content h1 {
        @apply mb-6;
    }

    .markdown-content h2 {
        @apply mt-12 mb-5;
    }

    .markdown-content h3 {
        @apply mt-10 mb-4;
    }

    .markdown-content h4,
    .markdown-content h5,
    .markdown-content h6 {
        @apply mt-8 mb-3;
    }

    .markdown-content p {
        @apply leading-[1.7] mb-5 last:mb-0;
    }

    .markdown-content pre {
        @apply p-4 mb-4;
    }

    .markdown-content a {
        @apply text-gray-800 underline;
    }

    .markdown-content ul,
    .markdown-content ol {
        @apply ml-6 mb-5;
    }

    .markdown-content blockquote {
        @apply border-l-4 border-gray-900 pl-4 text-gray-900 my-6 italic;
    }

    .markdown-content li {
        @apply leading-relaxed list-inside mb-2 last:mb-0;
    }

    .markdown-content img {
        @apply max-w-full max-h-full w-auto h-auto block my-8;
    }
</style>
