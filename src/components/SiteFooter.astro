<footer id="contact" class="site-footer">
    <div class="container footer-container section-padding-block">
        <div class="contact-text">
            <h2>Let's Work Together</h2>
            <p>
                Ready to get started? Fill out the form, and I’ll get back to
                you within 24 hours with details on how we can move forward.
            </p>
            <p>
                You can also <a
                    href="mailto:hanifcarroll@gmail.com?subject=Project Inquiry"
                    class="text-link">send me an email</a
                > or <a href="#" class="text-link">schedule a call</a>.
            </p>
        </div>

        <form class="contact-form">
            <div class="form-group">
                <label for="name">Name</label>
                <input
                    type="text"
                    id="name"
                    name="name"
                    placeholder="Your name"
                    autocomplete="name"
                    required
                />
            </div>
            <div class="form-group">
                <label for="email">Email</label>
                <input
                    type="email"
                    id="email"
                    name="email"
                    placeholder="your@email.com"
                    autocomplete="email"
                    required
                />
            </div>
            <div class="form-group">
                <label for="project-type">Project Type</label>
                <select id="project-type" name="project-type" required>
                    <option value="" disabled selected
                        >Select project type</option
                    >
                    <option value="web-design">Web Design</option>
                    <option value="web-development">Web Development</option>
                    <option value="branding">Branding</option>
                    <option value="other">Other</option>
                </select>
            </div>
            <div class="form-group">
                <label for="budget-range">Budget Range</label>
                <select id="budget-range" name="budget-range" required>
                    <option value="" disabled selected
                        >Select budget range</option
                    >
                    <option value="<5k">&lt; $5,000</option>
                    <option value="5k-10k">$5,000 - $10,000</option>
                    <option value="10k-20k">$10,000 - $20,000</option>
                    <option value=">20k">&gt; $20,000</option>
                </select>
            </div>
            <div class="form-group">
                <label for="project-description">Project Description</label>
                <textarea
                    id="project-description"
                    name="project-description"
                    rows="5"
                    required></textarea>
            </div>
            <button type="submit" class="btn btn-secondary">Send Message</button
            >
        </form>
    </div>

    <div class="footer-bottom container">
        <p class="text-body-small">&copy; Hanif Carroll 2025</p>
        <a
            href="https://www.linkedin.com/in/hanifcarroll/"
            target="_blank"
            rel="noopener noreferrer"
            aria-label="LinkedIn Profile"
        >
            <svg
                xmlns="http://www.w3.org/2000/svg"
                width="24"
                height="24"
                viewBox="0 0 24 24"
                fill="currentColor"
                ><path
                    d="M19 0h-14c-2.761 0-5 2.239-5 5v14c0 2.761 2.239 5 5 5h14c2.762 0 5-2.239 5-5v-14c0-2.761-2.238-5-5-5zm-11 19h-3v-11h3v11zm-1.5-12.268c-.966 0-1.75-.79-1.75-1.764s.784-1.764 1.75-1.764 1.75.79 1.75 1.764-.783 1.764-1.75 1.764zm13.5 12.268h-3v-5.604c0-3.368-4-3.113-4 0v5.604h-3v-11h3v1.765c1.396-2.586 7-2.777 7 2.476v6.759z"
                ></path></svg
            >
        </a>
    </div>
</footer>

<style>
    .site-footer {
        background-color: var(--primary-500);
        color: var(--secondary-500);
    }

    .site-footer a {
        color: var(--secondary-500);
        text-decoration: underline;
    }

    .footer-container {
        display: grid;
        grid-template-columns: 1fr;
        gap: 40px;
    }

    .contact-text p:nth-of-type(2) {
        margin-top: 12px;
    }

    .contact-form {
        display: grid;
        gap: 12px;
    }

    .form-group {
        display: grid;
        gap: 2px;
    }

    .form-group label {
        font-weight: 500;
        color: var(--secondary-100);
        margin-bottom: 4px;
    }

    .contact-form input,
    .contact-form select,
    .contact-form textarea {
        padding: 0.5rem;
        border: 1px solid var(--primary-300);
        border-radius: var(--border-radius);
        background-color: var(--primary-400);
        color: var(--secondary-500);
        font-size: var(--text-base);
        width: 100%;
        box-sizing: border-box;
        transition:
            border-color 0.3s ease,
            box-shadow 0.3s ease;
    }

    .contact-form select option {
        background-color: var(--primary-400);
        color: var(--secondary-100);
    }

    .contact-form textarea {
        resize: vertical;
        min-height: 120px;
    }

    .contact-form input::placeholder,
    .contact-form textarea::placeholder {
        color: var(--secondary-700);
    }

    .contact-form button {
        cursor: pointer;
        margin-top: 8px;
        font-size: 1rem;
    }

    .footer-bottom {
        background-color: var(--primary-500);
        color: var(--secondary-600);
        border-top: 1px solid var(--primary-400);
        display: flex;
        gap: 12px;
        align-items: center;
        padding-block: 20px;
    }

    .footer-bottom svg {
        width: 16px;
        height: 16px;
        position: relative;
        top: 2px;
    }

    /* Responsive adjustments */
    @media (min-width: 768px) {
        .footer-container {
            grid-template-columns: 1fr 1fr;
            grid-template-rows: 1fr;
            gap: 60px;
        }

        .contact-text p:nth-of-type(2) {
            margin-top: 20px;
        }
    }
</style>
