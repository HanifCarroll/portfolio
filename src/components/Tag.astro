---
// Props: label (string), variant (string: 'primary-outline' | 'primary-solid' | 'secondary-outline' | 'secondary-solid')
const { label, variant = "primary-outline" } = Astro.props;
---


<span class={`inline-flex items-center justify-center py-1 px-2 rounded-lg border transition-all duration-200 font-normal leading-tight cursor-default text-base ${
    variant === 'primary-outline' ? 'border-gray-800 text-gray-800 bg-transparent' :
    variant === 'primary-solid' ? 'bg-gray-800 text-gray-500 border-gray-800' :
    variant === 'secondary-outline' ? 'border-gray-500 text-gray-500 bg-transparent' :
    variant === 'secondary-solid' ? 'bg-gray-400 text-gray-300 border-gray-400' :
    'border-gray-800 text-gray-800 bg-transparent'
}`}>
    {label}
</span>
