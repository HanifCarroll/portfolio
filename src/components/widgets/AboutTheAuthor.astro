---
import Author from '../Author.astro';

interface Props {
	compact?: boolean;
	showLabel?: boolean;
}

const { compact = false, showLabel = true } = Astro.props as Props;
---

<div class:list={['about-the-author', compact && 'about-the-author--compact']}>
	{showLabel && <h3 class='section-label'>// About Hanif</h3>}
	<Author picSize={compact ? 'sm' : 'md'} />
	<div class:list={['author-desc mt-4 space-y-3 text-[0.95rem] leading-7 text-zinc-600 dark:text-zinc-300', compact && 'mt-3 space-y-0']}>
		<p>
			Hanif builds MVPs, workflow automation, and useful AI systems for nontechnical founders and founder-led teams. Based in Buenos Aires, he helps clients narrow the first useful version, ship it, and leave with software their team can own.
		</p>
		{
			!compact && (
				<p>His main stack is TypeScript, React, Next.js, and Node.js. He is currently available for roadmap calls and focused build engagements.</p>
			)
		}
	</div>
</div>
