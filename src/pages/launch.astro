---
import BaseLayout from '../layouts/BaseLayout.astro';
import TestimonialBillboard from '../components/TestimonialBillboard.astro';
import { BOOK_CALL_URL } from '../consts';
import testimonials from '../../testimonials.json';
import monicaImage from '../assets/img/monica.jpeg';

const title = 'MVP Launches & Fractional Product Engineering';
const description =
	'MVP launches for nontechnical founders and fractional product engineering for founder-led teams that need senior scope, architecture, and shipping help.';
const calUrl = BOOK_CALL_URL;
const serviceTestimonial = testimonials[0];

const pathChoices = [
	{
		label: 'Product path',
		title: '4-Week Launch',
		anchor: 'mvp',
		summary: 'For founders who need the first useful version scoped, built, and live.',
		price: 'Starts at $10,000',
		signals: ['The product direction is still fuzzy.', 'You need scope, UX, build, and launch in one place.'],
		cta: 'Talk through a launch',
		trackLocation: 'launch_path_product'
	},
	{
		label: 'Fractional path',
		title: 'Fractional Product Engineering',
		anchor: 'fractional',
		summary: 'For founder-led teams that need senior product engineering help without adding a full-time hire.',
		price: 'Fractional retainers',
		signals: [
			'The team needs scope control and technical judgment.',
			'You need architecture, implementation, and shipping momentum in the same person.'
		],
		cta: 'Talk through fractional help',
		trackLocation: 'launch_path_fractional'
	}
];

const roadmapSteps = [
	{
		num: '01',
		title: 'Bring the idea or product constraint',
		body: 'No polished brief required. Bring the messy version.'
	},
	{
		num: '02',
		title: 'Pick the smallest useful step',
		body: 'MVP launch, fractional product engineering, or a smaller scoping step.'
	},
	{
		num: '03',
		title: 'Leave with a practical scope',
		body: 'A starting path, likely budget range, and the next decision.'
	}
];

const proofItems = [
	{ label: '8+ years', value: 'Building software' },
	{ label: '10+ shipped', value: 'Products and systems' },
	{ label: 'Direct', value: 'Senior execution' }
];

const faqItems = [
	{
		title: 'Not sure which path fits?',
		body: 'That is what the roadmap call is for. I will tell you whether this should start as a focused MVP launch, fractional product engineering, or something smaller.'
	},
	{
		title: 'How does pricing work?',
		body: '4-Week Launches start at $10,000. Fractional product engineering is scoped around the amount of senior support the team needs each week.'
	},
	{
		title: 'What if the build should be smaller?',
		body: 'We will choose the simplest useful version. Sometimes that means cutting scope, improving the handoff, or deferring features until the product signal is clearer.'
	}
];
---

<BaseLayout title={`${title} · Hanif Carroll`} description={description} showFooterCta={false} footerLinksOnly bodyClass='page-launch'>
	<article class='container max-w-6xl service-page-stack py-14 md:py-16'>
		<nav class='flex items-center gap-2 font-mono text-xs uppercase tracking-wide'>
			<a href='/' class='transition-colors hover:text-primary'>Home</a>
			<span class='text-zinc-400'>/</span>
			<span class='text-primary'>Services</span>
		</nav>

		<header class='service-hero-grid'>
			<div class='service-hero-copy'>
				<h1 class='heading-display heading-display--hero' data-fit-heading data-fit-min-rem='2' data-fit-max-rem='4.85'>
					Launch the right first product.
				</h1>
				<p class='max-w-3xl text-xl leading-9 text-zinc-600 dark:text-zinc-300'>
					I help nontechnical founders turn unclear product ideas into focused web and mobile MVPs, and I help founder-led teams keep shipping with senior fractional product engineering.
				</p>
				<div class='flex flex-wrap gap-4'>
					<a
						href={calUrl}
						target='_blank'
						rel='noopener noreferrer'
						class='home-hero-primary-link'
						data-track-event='book_call_clicked'
						data-track-location='launch_hero'
					>
						Book a Roadmap Call
					</a>
				</div>
			</div>
		</header>

		<section class='service-section'>
			<TestimonialBillboard
				quote={serviceTestimonial.billboardText ?? serviceTestimonial.text}
				name={serviceTestimonial.name}
				role={serviceTestimonial.role}
				company={serviceTestimonial.company}
				image={monicaImage}
				label='Client note'
				class='testimonial-billboard--launch service-testimonial'
			/>
		</section>

		<div class='service-proof-strip'>
			{
				proofItems.map((item) => (
					<div class='service-proof-item'>
						<p class='launch-meta text-primary'>{item.label}</p>
						<p class='text-lg leading-8 text-zinc-600 dark:text-zinc-300'>{item.value}</p>
					</div>
				))
			}
		</div>

		<section id='paths' class='service-section'>
			<div class='service-choice-header'>
				<div class='service-choice-heading'>
					<p class='section-label'>// Two Ways To Start</p>
					<h2 class='heading-display heading-display--section service-choice-title'>
						<span>Unclear product?</span>
						<span>Launch first.</span>
						<span>Need senior help?</span>
						<span>Go fractional.</span>
					</h2>
				</div>
			</div>

			<div class='service-path-grid'>
				{
					pathChoices.map((choice) => (
						<a
							id={choice.anchor}
							href={calUrl}
							target='_blank'
							rel='noopener noreferrer'
							class='service-path-option group'
							data-track-event='book_call_clicked'
							data-track-location={choice.trackLocation}
						>
							<div class='service-path-option__meta'>
								<p class='launch-meta text-primary'>{choice.label}</p>
								<p class='launch-meta'>{choice.price}</p>
							</div>
							<div class='service-path-option__body'>
								<div class='space-y-3'>
									<h3 class='heading-display heading-display--subsection'>{choice.title}</h3>
									<p class='max-w-2xl text-lg leading-8 text-zinc-600 dark:text-zinc-300'>{choice.summary}</p>
								</div>
								<ul class='service-path-option__signals'>
									{choice.signals.map((signal) => (
										<li>{signal}</li>
									))}
								</ul>
								<span class='arrow-link w-fit group-hover:border-primary group-hover:text-primary'>{choice.cta}</span>
							</div>
						</a>
					))
				}
			</div>
		</section>

		<section class='service-section'>
			<div class='max-w-3xl space-y-4'>
				<p class='section-label'>// Roadmap Call</p>
				<h2 class='heading-display heading-display--section'>Use the call to pick the next move.</h2>
			</div>
			<div class='service-launch-steps'>
				{
					roadmapSteps.map((step) => (
						<div class='service-launch-step'>
							<p class='launch-meta text-primary'>Step {step.num}</p>
							<div class='space-y-3'>
								<h3 class='heading-display heading-display--subsection'>{step.title}</h3>
								<p class='text-lg leading-8 text-zinc-600 dark:text-zinc-300'>{step.body}</p>
							</div>
						</div>
					))
				}
			</div>
		</section>

		<section class='service-section'>
			<div class='max-w-3xl space-y-4'>
				<p class='section-label'>// Common Questions</p>
				<h2 class='heading-display heading-display--section'>A few things worth answering upfront.</h2>
			</div>
			<div class='service-faq-compact'>
				{
					faqItems.map((item) => (
						<div class='service-faq-compact__item'>
							<p class='service-faq-compact__question'>{item.title}</p>
							<p class='text-lg leading-8 text-zinc-600 dark:text-zinc-300'>{item.body}</p>
						</div>
					))
				}
			</div>
		</section>

		<section class='service-section service-detail-grid'>
			<div class='space-y-4'>
				<p class='section-label'>// Next Step</p>
				<h2 class='heading-display heading-display--section'>Bring the idea or product constraint.</h2>
			</div>
			<div class='space-y-5'>
				<p class='max-w-2xl text-lg leading-8 text-zinc-600 dark:text-zinc-300'>
					I&rsquo;ll tell you whether this should start as a focused MVP launch, fractional product engineering, or something smaller.
				</p>
				<a
					href={calUrl}
					target='_blank'
					rel='noopener noreferrer'
					class='home-hero-primary-link'
					data-track-event='book_call_clicked'
					data-track-location='launch_final'
				>
					Book a Roadmap Call
				</a>
			</div>
		</section>
	</article>
</BaseLayout>
