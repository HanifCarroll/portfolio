---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ProjectVideoCard from '../../components/project-video/ProjectVideoCard.astro';
import { projects } from '../../lib/project-cards';
import { PROJECT_INDEX_ORDER } from '../../lib/project-curation';

const title = 'Case Studies';
const description = 'Selected client and independent projects showing workflow diagnosis, product decisions, software systems, and supported results.';

const orderProjects = (projectList: typeof projects, preferredOrder: readonly string[]) => {
	const orderMap = new Map(preferredOrder.map((slug, index) => [slug, index]));

	return [...projectList].sort((a, b) => {
		const aIndex = orderMap.get(a.slug) ?? Number.MAX_SAFE_INTEGER;
		const bIndex = orderMap.get(b.slug) ?? Number.MAX_SAFE_INTEGER;
		return aIndex - bIndex;
	});
};

const orderedProjects = orderProjects(projects, PROJECT_INDEX_ORDER);
const leadProject = orderedProjects[0];
const priorityProjects = orderedProjects.slice(1, 5);
const compactProjects = orderedProjects.slice(5);
---

<BaseLayout title={`${title} | Hanif Carroll`} description={description} bodyClass='page-projects-index'>
	<article class='projects-index'>
		<header class='projects-index__hero' data-motion-intro>
			<div class='projects-index__hero-main hc-container-wide'>
				<p class='projects-index__label' data-motion-intro-item>Selected systems, launches, and operating tools</p>
				<h1 class='heading-display heading-display--hero' data-motion-headline>Proof is in what changed.</h1>
				<p class='projects-index__intro' data-motion-deck>These case studies show the workflow, decisions, systems, and evidence behind client and independent projects.</p>
			</div>
		</header>

		{
			leadProject && (
				<section class='projects-index__ranked-work hc-container-wide' aria-label='Ranked project work' data-motion-section>
					<ProjectVideoCard project={leadProject} variant='lead' />

					{
						priorityProjects.length > 0 && (
							<div class='projects-index__priority-grid'>
								{priorityProjects.map((project) => (
									<ProjectVideoCard project={project} variant='card' />
								))}
							</div>
						)
					}

					{
						compactProjects.length > 0 && (
							<div class='projects-index__compact-list'>
								{compactProjects.map((project) => (
									<ProjectVideoCard project={project} variant='compact' />
								))}
							</div>
						)
					}
				</section>
			)
		}
	</article>
</BaseLayout>
